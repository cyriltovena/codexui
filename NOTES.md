# Notes

Random notes and scratch-pad for the codexui project.

## Ideas / TODOs

- [ ] Add dark-mode toggle to the toolbar
- [ ] Investigate WebSocket reconnect logic on network drop
- [ ] Consider adding a keyboard-shortcut help overlay (press `?`)

## Dev Tips

- Run `npm run dev` to start the Vite dev server (default port 5173).
- Run `npm run build` to produce a production build in `dist/` and `dist-cli/`.
- The CLI entry point lives in `src/cli/` and is bundled via `tsup`.

## Misc

This file is intentionally informal — feel free to add quick notes here
instead of opening a full issue.
